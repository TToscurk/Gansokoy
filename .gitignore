node_modules/
shots/
shots2/

# 清理備份區 —— 不上傳（1.7GB，本地保險用）
work_backups/

# 2026-08-26 方向反轉：真實 skill 檔案在 .claude/skills/（Skill tool 需要），
# .agents/skills/ 是指向它的 Windows Junction（Codex 讀檔穿透無礙）。
# junction 側排除，避免 git 提交重複內容。
.agents/skills/

# 未被任何場景引用的大型模型下載（176/185MB，超過 GitHub 單檔上限）
godot/imported_models/Mountain Garden Manor/
godot/imported_models/Serene Edo Estate/

# Blender / Python generated files
__pycache__/
*.py[cod]
*.blend1

# Local AI / secret files
.qwen/
meshy_api_key.txt

# 本機工具產物 —— 不屬於專案本身。
# `godot/addons/` 是 godot-mcp 每次 `godot_launch` 用 copytree 覆寫進來的，
# 權威來源在 D:\godot-mcp，不是這個 repo；改這裡的副本會被下次啟動蓋掉。
godot/addons/
godot/.mcp.json
godot/.cursor/
.codex/
godot/*.log
