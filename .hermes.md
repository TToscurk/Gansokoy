# 幻想鄉緣一計畫 — Hermes 專案入口

本檔只負責讓 Hermes 在此 repo 載入正確的專案權威，不重複維護完整規則。

## 開工前必讀

每個任務開始前，依序讀取並遵守：

1. `AGENT_CONSTITUTION.md` — 本專案最高生產權威。
2. `AGENTS.md` — 跨代理工作規則與 skill 路由。
3. `CLAUDE.md` — 專案細節、工作流程與 reference 路由。

使用者當下的明確指令可凌駕特定條文；除此之外不得跳過上述文件。

## 工具與證據入口

- `graphify-out/graph.json` 存在時，專案內容、文件、架構或依賴問題先載入 `graphify` skill 並查圖，再回到原始文件或原始碼複核。
- Graphify 是索引，不是實況權威；`INFERRED`／`AMBIGUOUS` 關係不得當成事實。
- Godot 場景、節點、資源、匯入與 runtime 狀態，以 godot-ai MCP 為權威。
  MCP 負責 inspect／measure／modify／debug／verify；截圖是視覺證據，不是量測來源。
  可量測的值不得從截圖猜測；主觀視覺品質不得自我批准（見憲法第 5 條）。
- mesh／GLB 頂點、原點、尺度、行走面與幾何狀態，以 Blender MCP／`godot/tools/asset_probe.py` 為權威。
- GDToolkit 只負責 GDScript 靜態 lint、format 與 complexity，不能取代 Godot 實際載入、執行或視覺驗收。

## Godot 與美術紅線

- 任何觸及 Godot 的工作，先載入專案的 `godot-master`，並只讀本次工作需要的單一 reference。
- 場景／節點／資源狀態先用 godot-ai MCP 查實況，不從檔名、文件或記憶推測。
- 編輯器場景與磁碟不一致時，先存檔並重開專案／場景再修改，避免覆蓋使用者手調內容。
- 視覺只有使用者能批准；agent 至多到 `ART_REVIEW`。
- 資產流程：Meshy RAW → Blender 整理／頂點量測 → GLB → Godot 匯入 → prototype → render → Human Art Review → `ART_APPROVED` 後 rollout。
- `godot/assets/blender/sources/` 唯讀；未批准不得大量部署。
- 概念圖是美術權威，文字轉錄只是索引。

如本檔與 `AGENT_CONSTITUTION.md` 衝突，以憲法為準。
